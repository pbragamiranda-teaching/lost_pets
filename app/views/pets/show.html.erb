
<h1><%= @pet.name %>, the <%= @pet.species %></h1>

<h2>Found on <%= @pet.found_on %> at <%= @pet.address %> </h2>


<%= link_to "Edit", edit_pet_path(@pet) %>
<%= link_to "Delete", delete_pet_path(@pet), method: :delete , data: { confirm: "are you sure?"} %>
<%= link_to "Back",  pets_path %>
